# ============================================
# CashFlow v4.0 - Backend Environment Variables
# ============================================

# Environment
NODE_ENV=development

# Server
PORT=5000

# Database
MONGODB_URI=mongodb://localhost:27017/cashflow-dev

# JWT Secret (generate a secure one for production)
# Use: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-super-secure-jwt-secret-here-change-in-production

# OpenAI API (optional)
OPENAI_API_KEY=your-openai-api-key-here

# CORS
CORS_ORIGIN=http://localhost:3000

# ============================================
# PRODUCTION SETTINGS (when deploying)
# ============================================

# NODE_ENV=production
# PORT=5000
# MONGODB_URI=mongodb://localhost:27017/cashflow-production
# JWT_SECRET=<generate-secure-64-char-secret>
# OPENAI_API_KEY=<your-production-key>
# CORS_ORIGIN=https://your-domain.com

# ============================================
# SECURITY NOTES
# ============================================
# 1. NEVER commit .env files to git
# 2. Always use strong, unique JWT_SECRET in production
# 3. Rotate secrets regularly
# 4. Keep OpenAI key secure and monitor usage
# 5. Use environment-specific CORS origins
